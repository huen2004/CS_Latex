\ChapterImageStar[cap:validacion]{Implementación y validación de la solución}{./images/fondo.png}\label{cap:validacion}
\mbox{}\\
La presente sección tiene como finalidad validar la efectividad de la arquitectura de defensa en profundidad implementada mediante la 
integración de OPNsense~\cite{OpnsenseManual2026}, Suricata~\cite{SuricataGuide2025} y Wazuh~\cite{WazuhDocs2026} dentro de un laboratorio 
virtualizado. Para ello, se diseñaron seis casos de prueba basados en el marco MITRE ATT\&CK~\cite{MITREATTACK2026} y las fases de la Cyber 
Kill Chain~\cite{CyberKillChain2026}, permitiendo evaluar capacidades de detección, correlación de eventos y respuesta activa frente a 
diferentes escenarios de ataque.

Las pruebas fueron ejecutadas desde una máquina atacante con OpenSUSE, generando tráfico y eventos maliciosos controlados dentro de un 
entorno completamente aislado. De esta manera, fue posible analizar el comportamiento de la arquitectura propuesta y validar la capacidad 
de monitoreo continuo, mitigación dinámica y gestión centralizada de eventos de seguridad.

\section{Metodología de ejecución de pruebas}
\noindent

Con el fin de validar los aspectos de seguridad integrados mediante el modelo de arquitectura de defensa en profundidad implementado, se 
diseñó una metodología orientada a garantizar la reproducibilidad y la validez científica de los resultados obtenidos en cada uno de los 
seis (6) escenarios de prueba. Para ello, se estableció un protocolo estandarizado de ejecución compuesto por las siguientes fases:

\begin{itemize}
    \item \textbf{Objetivo de la prueba:} define el resultado esperado de la prueba, especificando los componentes del modelo de defensa en profundidad que se buscan validar y las condiciones bajo las cuales se ejecutará el escenario.
    
    \item \textbf{Herramienta utilizada:} describe la herramienta empleada y los parámetros de configuración utilizados durante la prueba, proporcionando un contexto de reproducibilidad técnica.
    
    \item \textbf{Ejecución de la prueba:} expone el procedimiento realizado, incluyendo el comando o herramienta ejecutada desde la máquina atacante y las observaciones efectuadas durante el desarrollo de la prueba.
    
    \item \textbf{Resultados y evidencias:} documenta los hallazgos obtenidos mediante capturas de pantalla, registros y métricas temporales, permitiendo realizar un análisis objetivo del comportamiento de la arquitectura de defensa en profundidad.
    
    \item \textbf{Análisis del ciclo de detección:} evalúa el desempeño integral del sistema de detección y respuesta frente al ataque simulado, determinando el estado final del caso de prueba como \textit{Aprobado} o \textit{Rechazado}.
\end{itemize}

\subsection{Criterios de validación}
\noindent

Con el propósito de evaluar cuantitativa y cualitativamente el desempeño de la arquitectura propuesta, se definieron criterios estrictos 
para determinar el resultado final de cada caso de prueba.

\subsubsection{Criterio de Éxito (Aprobado)}

Una prueba se considera exitosa si cumple de manera secuencial la totalidad del ciclo de automatización definido:

\begin{itemize}
    \item El tráfico malicioso es interceptado por Suricata.
    
    \item El agente reporta el evento al Wazuh Manager.
    
    \item El mánager correlaciona la regla y genera la alerta correspondiente en el dashboard.
    
    \item El agente de OPNsense inyecta de forma automática la IP atacante en la tabla de bloqueo, interrumpiendo la conectividad en un tiempo menor o igual a 40 segundos desde la detección del evento.
\end{itemize}

En algunos escenarios, la herramienta \SIEM\ Wazuh puede no ejecutar la respuesta activa hacia el firewall. En dichos casos, es necesario 
verificar el nivel de criticidad asignado a la alerta generada, debido a que eventos clasificados como riesgo mínimo o con un nivel 
inferior a 8 en la escala de prioridad de Wazuh no desencadenan mecanismos automáticos de intervención.

\subsubsection{Criterio de Fallo (Rechazado)}

Una prueba se considera fallida cuando se presenta alguna de las siguientes anomalías:

\begin{itemize}
    \item El ataque evade las firmas del \IDS\ o los registros del sistema (\textit{falso negativo}).
    
    \item El agente no logra transmitir la alerta al Wazuh Manager.
    
    \item El mánager no procesa correctamente la regla de respuesta activa.
    
    \item El firewall OPNsense no aplica el bloqueo perimetral, permitiendo que el atacante complete la explotación.
\end{itemize}


\section{Desarrollo de los casos de prueba}
\noindent

El propósito de este apartado es consolidar y describir los seis (6) casos de prueba ejecutados sobre el entorno virtualizado en el que se 
implementó la arquitectura de defensa en profundidad. En cada escenario se especifica el origen del ataque, la técnica empleada y el 
comportamiento de los componentes de seguridad desplegados, evaluando la capacidad de detección del \IDPS\ Suricata, la correlación de 
eventos realizada por el \SIEM Wazuh y la aplicación de medidas de contención mediante la respuesta activa integrada con el firewall 
OPNsense.

Lo anterior permite determinar la efectividad de los aspectos de seguridad implementados y su alineación con los lineamientos establecidos 
en las normas \ISO/IEC 27001:2022 e \ISO/IEC 27017:2015, verificando la capacidad de la infraestructura para proteger los activos y 
servicios del grupo \GRID\ frente a diferentes escenarios de amenaza. A continuación, se presenta la descripción de cada una de las 
pruebas realizadas.

\subsection{Configuración del entorno de red para la ejecución de casos de prueba}
\noindent

Para el desarrollo de los casos de prueba descritos a continuación, se implementaron dos reglas \DNAT\ (Destination Network Address 
Translation) en el firewall OPNsense, con el propósito de exponer de forma controlada los servicios SSH y HTTP de la red interna hacia la 
red WAN simulada. En ambas reglas se definió el protocolo TCP, debido a que dichos servicios operan exclusivamente sobre este protocolo, 
siguiendo el principio de mínimo privilegio.

Esta configuración permitió redirigir el tráfico entrante dirigido a la interfaz WAN hacia la máquina objetivo ubicada en la red de 
servidores internos, replicando un escenario real de exposición de servicios hacia redes externas no confiables. De esta manera, fue 
posible ejecutar ataques controlados sobre la máquina objetivo y evaluar la capacidad de detección, correlación y mitigación de amenazas 
por parte de la arquitectura de seguridad implementada. En la figura~\ref{fig1} se demuestra la creación y parámetros de la virtual IP y en 
la figura~\ref{fig2} se evidencia la configuración de las reglas \DNAT\ establecidas.


\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-1.jpeg}
	\caption{Configuración de virtual IP}\label{fig1}
\end{figure}


\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-2.jpeg}
	\caption{Configuración de las reglas DNAT}\label{fig2}
\end{figure}

Adicionalmente, fue necesario deshabilitar las opciones de bloqueo de redes privadas y redes Bogon en la interfaz WAN de OPNsense, debido a 
que el entorno virtualizado del laboratorio emplea direccionamiento IP privado en los segmentos de red utilizados. Esta configuración 
permitió habilitar el tránsito del tráfico entre la red atacante y la máquina objetivo, garantizando la correcta ejecución de los escenarios 
de prueba. En la figura~\ref{fig3} se evidencia la deshabilitación de las opciones \textit{Block private networks} y \textit{Block bogon 
networks}.

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-3.jpeg}
	\caption{Deshabilitación de opciones de bloqueo de tráfico de red}\label{fig3}
\end{figure}

Asimismo, durante la configuración de las reglas \DNAT\ previamente definidas, se habilitó la opción de creación automática de reglas de 
firewall asociadas a cada traducción realizada sobre la interfaz WAN. Esto permitió autorizar de manera automática el tránsito del 
tráfico de red hacia los servicios expuestos, evitando la necesidad de crear reglas manuales adicionales. La figura~\ref{fig4} presenta el 
contexto visual de la configuración aplicada.

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-4.jpeg}
	\caption{Reglas creadas automáticamente a partir de las DNAT}\label{fig4}
\end{figure}

De igual forma, en la Figura~\ref{fig5} se evidencia la adecuación de las reglas necesarias para permitir el tránsito desde la interfaz 
SERVERS hacia los servicios SSH y HTTP mediante la dirección IP virtual especificada anteriormente. Esta configuración tuvo como finalidad 
habilitar la ejecución de las pruebas y simular de manera adecuada la exposición controlada de servicios por parte de la máquina objetivo.

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-5.jpeg}
	\caption{Reglas creadas automáticamente para la interfaz SERVERS}\label{fig5}
\end{figure}

%---------------------------------------------------------------------------------------------------------------------------------------

\subsection{Caso de Prueba 1 (CP-01) Escaneo de Puertos Mediante Nmap}
\noindent

Para la primera prueba, se establece que la fase de reconocimiento constituye la etapa inicial de cualquier ataque informático, en la cual 
el adversario identifica servicios expuestos y posibles vectores de explotación sobre la infraestructura objetivo. En este caso de prueba, 
se simuló un actor externo ejecutando un escaneo de red desde la WAN hacia la dirección IP virtual del firewall OPNsense, con el propósito 
de validar la capacidad del sistema de detección y respuesta para identificar, alertar y mitigar de forma automatizada este tipo de 
actividad maliciosa.

\subsubsection{Objetivo de la prueba}
\noindent
Verificar que el sistema de defensa en profundidad implementado, compuesto por las herramientas de código abierto Wazuh, Suricata y 
OPNsense, detecte un escaneo de reconocimiento proveniente desde la red WAN, genere alertas asociadas a la firma identificada y ejecute la 
respuesta activa mediante el bloqueo perimetral automatizado una vez superado el umbral de tolerancia establecido. Dicho umbral se activa 
cuando se registran más de cinco alertas del mismo tipo en un intervalo de 60 segundos.

\subsubsection{Herramienta Utilizada}
\noindent

La herramienta utilizada fue Nmap (Network Mapper), una utilidad de código abierto ampliamente empleada en el ámbito de la seguridad 
informática para el descubrimiento de redes y la auditoría de seguridad. Esta permite identificar hosts activos, puertos abiertos, 
servicios en ejecución y sus respectivas versiones mediante el envío de paquetes construidos de manera específica~\cite{Nmap2026}.

Para este caso de prueba se utilizó el siguiente comando:

\begin{lstlisting}[language=bash, caption={Comando Nmap utilizado para el escaneo de reconocimiento}, label={lst:nmap_scan}]

sudo nmap -sS -Pn -sV -p 22,80 192.168.170

\end{lstlisting}

Donde cada parámetro cumple una función específica, tal como se describe en la Tabla~\ref{tab:parametros_nmap}.

\begin{table}[H]
\centering
\caption{Descripción de Parámetros para NMAP}\label{tab:parametros_nmap}
\begin{tabular}{|c|p{9cm}|}
\hline
\textbf{Parámetro} & \textbf{Descripción} \\ \hline

\texttt{-sS} & Realiza un escaneo SYN sigiloso, no completa el \textit{handshake} TCP, pero envía paquetes SYN a cada puerto. \\ \hline

\texttt{-Pn} & Omite la detección de host activo mediante \textit{ping}. \\ \hline

\texttt{-sV} & Detecta la versión de los servicios en los puertos encontrados mediante el envío de grandes volúmenes de paquetes luego de detectar un puerto abierto. \\ \hline

\texttt{-p 22,80} & Limita el escaneo a los puertos correspondientes a SSH y HTTP. \\ \hline

\end{tabular}
\end{table}

\subsubsection{Ejecución de la prueba}
\noindent

Este apartado se desarrolló desde la máquina atacante OpenSUSE, desde la cual se ejecutó un escaneo de reconocimiento sobre la dirección 
IP virtual configurada en OPNsense \texttt{192.168.1.70}, expuesta a través de la interfaz WAN del firewall. Para ello, se utilizó el 
comando descrito en el apartado anterior, replicando el comportamiento de un atacante real en un entorno donde el objetivo puede 
encontrarse protegido mediante reglas ICMP.

El escaneo inicial, evidenciado en la figura~\ref{fig6}, permitió identificar los servicios activos sobre los puertos 22 y 80, obteniendo 
información relacionada con las versiones del software en ejecución y el sistema operativo del objetivo. Asimismo, fue necesario utilizar 
el comando \texttt{date} para verificar el tiempo exacto en el que inició la ejecución del comando Nmap.

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-6.jpeg}
	\caption{Escaneo Inicial a la máquina objetivo}\label{fig6}
\end{figure}

Una vez ejecutado el comando Nmap, el resultado esperado corresponde al bloqueo de la dirección IP desde la cual se realizó el escaneo. 
Esto puede verificarse al intentar reiterar el mismo procedimiento posteriormente. En el siguiente apartado se evaluará el comportamiento 
interno de la arquitectura de defensa en profundidad y se verificará si cumple con los criterios establecidos previamente.

\subsubsection{Resultados y Evidencias}
\noindent

El escaneo inicial permitió al atacante identificar los servicios expuestos en la dirección IP virtual, obteniendo información sensible de 
la máquina objetivo. No obstante, el tráfico generado fue interceptado por Suricata, el cual produjo múltiples alertas de reconocimiento, 
entre las que se destaca la firma \textit{“ET SCAN Nmap Scripting Engine User Agent Detected”}, categorizada como \textit{Web Application 
Attack} y con un nivel de confianza \textit{High}. En la Figura~\ref{fig7} se evidencia el flujo de registros capturados por Suricata en el archivo 
\texttt{eve.json}.

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-7.jpeg}
	\caption{Escaneo Inicial a la máquina objetivo}\label{fig7}
\end{figure}

Una vez superado el umbral de tolerancia establecido, es decir, al generarse más de cinco alertas de reconocimiento por parte de Suricata, 
el Wazuh Manager correlacionó los eventos y activó la respuesta activa a las 12:40:51, ordenando al agente de OPNsense inyectar la 
dirección IP atacante \texttt{192.168.1.57} en la tabla de bloqueo \texttt{\_\_wazuh\_agent\_drop} a las 12:40:58.

El escaneo fue ejecutado a las 12:40:50, logrando la contención del ataque en un tiempo aproximado de 8 segundos desde el inicio del 
escaneo, valor considerablemente inferior al umbral crítico de 30 segundos definido en la metodología. En la figura~\ref{fig8} se evidencia la 
primera alerta reconocida por Wazuh y en la figura~\ref{fig9} el registro correspondiente a la respuesta activa ejecutada por OPNsense.

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-8.jpeg}
	\caption{Dashboard de Wazuh emitiendo alertas}\label{fig8}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-9.jpeg}
	\caption{Registro de Instrucción de Respuesta Activa en OPNsense}\label{fig9}
\end{figure}

Tras la activación automática del mecanismo de respuesta activa por parte de Wazuh, se procedió a ejecutar un segundo escaneo utilizando 
los mismos parámetros, con el propósito de verificar la efectividad del bloqueo perimetral aplicado por OPNsense. Los resultados obtenidos 
se presentan en la figura~\ref{fig10}.

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-10.jpeg}
	\caption{Ejecución de comando Nmap posterior a la respuesta activa}\label{fig10}
\end{figure}

El cambio de estado de \textit{“open”} a \textit{“filtered”} confirma que OPNsense descarta los paquetes provenientes del atacante debido 
al bloqueo aplicado mediante la respuesta activa. Adicionalmente, el tiempo de escaneo se redujo de 6.49 a 1.46 segundos, dado que Nmap no 
recibió respuesta alguna por parte del objetivo.

\subsubsection{Análisis del ciclo de detección}
\noindent

El caso de prueba permitió validar el funcionamiento integral del pipeline de detección y respuesta automatizada implementado en el 
laboratorio propuesto. El escaneo ejecutado desde la red WAN generó tráfico TCP SYN dirigido hacia los puertos 22 y 80 de la dirección IP 
virtual. Dicho tráfico fue capturado simultáneamente por Suricata a través de la interfaz en modo promiscuo conectada al \texttt{bridge0} 
de la red de OPNsense, garantizando visibilidad completa del tráfico y permitiendo la inspección profunda de paquetes en paralelo al 
procesamiento perimetral del firewall.

Las alertas generadas por Suricata fueron enviadas al Wazuh Manager mediante el agente instalado en la máquina del \IDPS\. Posteriormente, 
la regla personalizada \texttt{100003} correlacionó los eventos y elevó la alerta al nivel 12 una vez superado el umbral de cinco alertas 
consecutivas de reconocimiento, activando de forma automática el mecanismo de respuesta activa.

El ciclo completo de detección y contención se completó en 8 segundos desde la ejecución del escaneo, distribuyéndose aproximadamente en 
1.5 segundos para la detección y 6.5 segundos para la ejecución de la respuesta activa, cumpliendo satisfactoriamente el umbral crítico de 
30 segundos definido en la metodología.

El caso de prueba se clasifica como \textbf{Aprobado}, debido a que el ciclo completo de detección, correlación y bloqueo perimetral se 
ejecutó de manera secuencial y automatizada, impidiendo actividades posteriores de reconocimiento sobre el laboratorio virtual.

%-------------------------------------------------------------------------------------------------------------------------------------

\subsection{Caso de prueba 2 (CP-02) Ataque de Fuerza Bruta Hydra}
\noindent

Los ataques de fuerza bruta representan una de las amenazas más frecuentes contra los servicios de acceso remoto, consistiendo en el 
intento sistemático de múltiples combinaciones de credenciales hasta obtener acceso no autorizado. El servicio SSH constituye uno de los 
objetivos más comunes debido a su amplio uso en la administración remota de sistemas.

Para este caso de prueba se simuló un ataque de fuerza bruta desde la red WAN hacia la dirección IP virtual expuesta, con el propósito de 
validar la capacidad del sistema de detección y respuesta para identificar y mitigar la amenaza de manera automatizada.

\subsubsection{Objetivo de la prueba}
\noindent

Verificar que el modelo de defensa en profundidad implementado detecte un ataque de fuerza bruta dirigido al servicio SSH, genere alertas 
de tráfico malicioso por parte de Suricata y envíe los eventos correspondientes al Wazuh Manager, activando posteriormente el mecanismo de 
respuesta activa para bloquear la dirección IP atacante antes de que se logre un acceso no autorizado a la máquina objetivo.

\subsubsection{Herramienta Utilizada}
\noindent

La herramienta de código abierto Hydra~\cite{Hydra2026} es utilizada para ejecutar ataques de fuerza bruta contra servicios de autenticación y es 
ampliamente empleada en auditorías de seguridad para evaluar la robustez de las credenciales configuradas en sistemas expuestos. Esta 
herramienta soporta múltiples protocolos, entre los que se destacan SSH, FTP, HTTP y RDP.

Para este caso de prueba se empleó el siguiente comando de Hydra:

\begin{lstlisting}[language=bash, caption={Comando Hydra utilizado para el ataque de fuerza bruta SSH}, label={lst:hydra_ssh}]

hydra -l huendycaicedo -P ~/wordlist_ssh.txt 192.168.1.70 ssh -t 4 -v

\end{lstlisting}

Cada parámetro cumple una función específica, tal como se describe en la Tabla~\ref{tab:parametros_hydra}.

\begin{table}[H]
	\centering
	\caption{Descripción de parámetros utilizados en Hydra}\label{tab:parametros_hydra}
	\renewcommand{\arraystretch}{1.4}
	\begin{tabular}{|p{4.2cm}|p{8.5cm}|}
	\hline
	\rowcolor{gray!25}
	\centering \textbf{Parámetro} & \centering \textbf{Descripción} \tabularnewline
	\hline

	\centering \texttt{-l huendycaicedo} &
	Especifica el usuario de la máquina objetivo sobre el cual se realizará el ataque de fuerza bruta.
	\tabularnewline
	\hline

	\centering \texttt{-P \textasciitilde{}/wordlist\_ssh.txt} &
	Define el diccionario de contraseñas utilizado durante el proceso de autenticación automatizada.
	\tabularnewline
	\hline

	\centering \texttt{ssh} &
	Indica el protocolo objetivo sobre el cual se ejecutará el ataque.
	\tabularnewline
	\hline

	\centering \texttt{-t 4} &
	Establece el número de hilos paralelos utilizados para realizar múltiples intentos de conexión simultáneamente.
	\tabularnewline
	\hline

	\centering \texttt{-v} &
	Habilita el modo \textit{verbose}, permitiendo visualizar cada intento de autenticación en tiempo real.
	\tabularnewline
	\hline

	\end{tabular}
\end{table}

El diccionario de contraseñas utilizado fue personalizado e incluyó combinaciones de uso común, así como la credencial real del usuario 
objetivo. Esto permitió simular un escenario de ataque más cercano a un entorno real, garantizando la completitud y validez del caso de 
prueba. La composición del diccionario empleado se presenta en el siguiente apartado.

\subsubsection{Ejecución de la prueba}
\noindent

Desde la máquina atacante se ejecutó un ataque de fuerza bruta sobre el servicio SSH expuesto en la dirección IP virtual 
\texttt{192.168.1.70}, utilizando el diccionario personalizado de contraseñas descrito anteriormente y tomando como objetivo el usuario 
\texttt{huendycaicedo}. El ataque consistió en múltiples intentos automatizados de autenticación SSH, generando un volumen considerable de 
conexiones fallidas en un corto intervalo de tiempo.

Es importante resaltar que el archivo de contraseñas utilizado por la herramienta incluía la credencial real de la máquina objetivo, con el 
propósito de simular un escenario realista de compromiso exitoso mediante fuerza bruta y validar el comportamiento del sistema ante un 
intento de acceso no autorizado exitoso. En la Figura~\ref{fig11} se presenta una muestra del archivo \texttt{wordlist\_ssh.txt} utilizado durante 
la prueba.

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.4]{tablas-images/pruebas/vali-11.png}
	\caption{Diccionario de contraseñas Personalizado para Hydra}\label{fig11}
\end{figure}

Como se evidencia en la figura~\ref{fig12}, Hydra inició el ataque probando de forma secuencial cada una de las contraseñas contenidas en el 
diccionario configurado. Para efectos prácticos y de validación del laboratorio, se permitió la continuidad del ataque hasta identificar 
la credencial correcta y lograr un acceso exitoso mediante el servicio SSH, simulando así un escenario real de compromiso por fuerza bruta.

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-12.jpeg}
	\caption{Ejecución del comando Hydra}\label{fig12}
\end{figure}

\subsubsection{Resultados y Evidencias}
\noindent

El ataque de fuerza bruta dirigido al servicio SSH desde la máquina atacante generó un volumen considerable de intentos fallidos de 
autenticación hacia la dirección IP virtual \texttt{192.168.1.70}. Dicho tráfico fue interceptado por Suricata, el cual generó la alerta 
correspondiente a la firma \texttt{ET SCAN LibSSH Based Frequent SSH Connections Likely BruteForce Attack} con un nivel de confianza medio, 
identificando correctamente el patrón de comportamiento asociado a Hydra y al uso intensivo de la librería \texttt{libssh}. En la 
Figura~\ref{fig13} se evidencia la alerta generada por Suricata durante la ejecución del ataque.

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-13.jpeg}
	\caption{Registros de alerta Suricata}\label{fig13}
\end{figure}

El Wazuh Manager procesó la alerta generada a las 12:18:30 AM y activó el mecanismo de respuesta activa a las 12:18:31 AM, inyectando la 
dirección IP atacante \texttt{192.168.1.57} en la tabla de bloqueo \texttt{\_\_wazuh\_agent\_drop} de OPNsense. El ataque fue iniciado a 
las 12:17:56 AM, logrando su contención en un tiempo total aproximado de 35 segundos. En la Figura~\ref{fig14} se evidencia el dashboard de Wazuh 
con la alerta generada y la interacción con OPNsense, mientras que en la Figura~\ref{fig15} se observa la ejecución de la respuesta activa aplicada 
por el firewall.

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-14.jpeg}
	\caption{Dashboard de Wazuh con Alerta de SSH}\label{fig14}
\end{figure}

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-15.jpeg}
	\caption{Respuesta Activa en OPnsense}\label{fig15}
\end{figure}

Hydra no logró completar el ataque de fuerza bruta ni obtener acceso no autorizado a la máquina objetivo, debido a que el mecanismo de 
bloqueo perimetral interrumpió la comunicación antes de que se agotara el diccionario de contraseñas utilizado durante la prueba. En la 
Figura~\ref{fig16} se evidencia la interrupción del ataque tras la activación de la respuesta activa por parte de la arquitectura de 
defensa implementada.

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-16.jpeg}
	\caption{Intento de ataque con Hydra Posterior al Bloqueo}\label{fig16}
\end{figure}

\subsubsection{Análisis del ciclo de detección}
\noindent

El caso de prueba permitió validar el comportamiento de la arquitectura de defensa en profundidad frente a un ataque de fuerza bruta 
dirigido al servicio SSH. La detección fue realizada por Suricata, el cual identificó el patrón de comportamiento característico asociado 
a la librería \texttt{libssh} utilizada internamente por Hydra, generando la alerta correspondiente a la firma \texttt{ET SCAN LibSSH Based 
Frequent SSH Connections Likely BruteForce Attack}.

El tráfico generado durante el ataque fue capturado simultáneamente por Suricata mediante la interfaz configurada en modo promiscuo y 
conectada al bridge de red de OPNsense, permitiendo realizar la inspección del patrón de conexiones SSH en paralelo al procesamiento 
perimetral del firewall. Posteriormente, la alerta fue enviada al Wazuh Manager a través del agente desplegado en la máquina Suricata, 
donde el grupo de reglas asociado activó el mecanismo de respuesta activa al detectar la firma correspondiente.

El ciclo completo de detección y contención se ejecutó en un tiempo total aproximado de 35 segundos desde el inicio del ataque, 
distribuidos en 32 segundos de detección y 3 segundos de respuesta activa, manteniéndose por debajo del umbral crítico definido en la 
metodología. El tiempo de detección se encuentra condicionado por el umbral de acumulación de conexiones SSH requerido por Suricata para 
generar la firma correspondiente, permitiendo reducir la probabilidad de falsos positivos asociados a conexiones SSH legítimas y 
esporádicas.

El caso de prueba se clasifica como \textbf{Aprobado}, debido a que el sistema detectó, alertó y bloqueó el ataque de fuerza bruta de 
forma automatizada, impidiendo que Hydra completara el proceso de autenticación y obtuviera acceso no autorizado a la máquina objetivo, 
cumpliendo satisfactoriamente el ciclo de detección y respuesta definido en la metodología.

%-------------------------------------------------------------------------------------------------------------------------------------

\subsection{Caso de Prueba 3 (CP-03) Reconocimiento de vulnerabilidades Nikto}
\noindent

En un ejercicio de seguridad ofensiva, el escaneo de aplicaciones web representa uno de los vectores iniciales más comunes. Los 
servidores web exponen superficies de ataque amplias que pueden ser aprovechadas por un atacante durante las primeras etapas de 
reconocimiento~\cite{MITREATTACK2026}. En este caso, se empleó una técnica de reconocimiento de vulnerabilidades mediante un escaneo web 
activo contra la aplicación \textit{Damn Vulnerable Web Application} (DVWA), desplegada en la máquina objetivo, con el propósito de 
verificar que la arquitectura de defensa en profundidad implementada es capaz de detectar, generar alertas y mitigar este tipo de 
actividad de forma automatizada.

\subsubsection{Objetivo de la prueba}
\noindent

Validar la capacidad de detección y respuesta de la arquitectura de defensa en profundidad implementada ante un escaneo web activo 
ejecutado desde la máquina atacante hacia la máquina objetivo. Para ello, se busca verificar que Suricata identifique los patrones de 
tráfico HTTP asociados al escáner, que Wazuh procese y correlacione los eventos generados asignando el nivel de severidad correspondiente, 
y que el mecanismo de respuesta activa ejecute el bloqueo automatizado de la dirección IP atacante en OPNsense, completando el ciclo de 
detección, correlación y contención definido en la metodología.

\subsubsection{Herramienta Utilizada}
\noindent

Nikto es un escáner de vulnerabilidades web de código abierto desarrollado en Perl y mantenido activamente desde 2001. La herramienta 
ejecuta más de 6\,700 pruebas automatizadas sobre el servidor objetivo, incluyendo la detección de archivos y directorios sensibles, la 
identificación de versiones de software con vulnerabilidades conocidas, el análisis de cabeceras HTTP inseguras y la detección de 
configuraciones incorrectas~\cite{NiktoDocs}. Debido a su naturaleza altamente visible y a la ausencia de mecanismos de evasión 
habilitados por defecto, Nikto es ampliamente utilizado en auditorías y evaluaciones de seguridad de aplicaciones web. A continuación, se 
presenta el comando utilizado para la ejecución de esta prueba:

\begin{lstlisting}[language=bash, caption={Comando utilizado para la ejecución de Nikto}, label={lst:nikto}]

nikto -h http://192.168.1.70/DVWA

\end{lstlisting}

Cada parámetro cumple una función específica, tal como se describe en la Tabla~\ref{tab:parametros_nikto}.

\begin{table}[H]
	\centering
	\caption{Descripción de parámetros utilizados en Nikto}\label{tab:parametros_nikto}
	\renewcommand{\arraystretch}{1.4}
	\begin{tabular}{|p{4.2cm}|p{8.5cm}|}
	\hline
	\rowcolor{gray!25}
	\centering \textbf{Parámetro} & \centering \textbf{Descripción} \tabularnewline
	\hline

	\centering \texttt{-h http://192.168.1.70/DVWA} &
	Define el host objetivo al cual se le van a realizar los escáneres de vulnerabilidades web mediante las pruebas automatizadas
	\tabularnewline
	\hline
	\end{tabular}
\end{table}

\subsubsection{Ejecución de la prueba}
\noindent

Esta prueba se ejecutó desde la máquina atacante mediante el uso de Nikto contra la dirección IP virtual \texttt{192.168.1.70}, 
expuesta en la interfaz WAN del firewall OPNsense. Para ello, se empleó el comando descrito en el apartado anterior, replicando el 
comportamiento de un atacante que busca identificar vulnerabilidades y configuraciones inseguras en una aplicación web accesible desde 
una red externa. El escaneo, presentado en la Figura~\ref{fig17}, se inició a las 19:06:50. En este contexto, el uso del comando \texttt{date} 
permitió registrar con precisión el instante de inicio de la prueba, facilitando posteriormente el análisis de los tiempos de detección y 
respuesta de la arquitectura de defensa en profundidad.

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-17.jpeg}
	\caption{Escaneo de vulnerabilidades Http}\label{fig17}
\end{figure}

Nikto identificó al servidor \textit{Apache/2.4.67} sobre Debian como la plataforma subyacente de la aplicación DVWA. Asimismo, reportó 
la ausencia de cinco cabeceras de seguridad HTTP consideradas fundamentales: \textit{Referrer-Policy}, \textit{Content-Security-Policy}, 
\textit{X-Content-Type-Options}, \textit{Permissions-Policy} y \textit{Strict-Transport-Security}. La ausencia de estas cabeceras puede 
incrementar la superficie de ataque de la aplicación, facilitando la explotación de vulnerabilidades asociadas a la ejecución de 
contenido no confiable, el manejo inseguro de recursos web y otros vectores de ataque dirigidos al cliente.

\subsubsection{Resultados y Evidencias}
\noindent

Los resultados obtenidos durante esta prueba confirman el funcionamiento integral del ciclo de detección y respuesta implementado en la 
arquitectura de defensa en profundidad ante un escaneo web activo.

Como se evidencia en la Figura~\ref{fig18}, Suricata detectó el tráfico generado por Nikto a las 19:07:05, generando múltiples alertas asociadas a 
la firma \textit{ET WEB\_SERVER Script tag in URI Possible Cross Site Scripting Attempt}. Dichas alertas registraron conexiones 
provenientes de la dirección IP atacante \texttt{192.168.1.57} hacia la dirección IP objetivo \texttt{10.0.0.2} a través del puerto 80. 
Los eventos fueron capturados en la interfaz \texttt{ens36} y procesados por el agente de Suricata, confirmando que el motor de detección 
identificó correctamente los patrones de tráfico potencialmente malicioso generados por el escaneo web.

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-18.jpeg}
	\caption{Reconocimiento de alertas Http en Suricata}\label{fig18}
\end{figure}

Como evidencia del proceso de correlación y respuesta, la Figura~\ref{fig19} muestra que Wazuh recibió y procesó las alertas generadas por 
Suricata a las 19:07:05.370. Posteriormente, el mecanismo de respuesta activa ordenó el bloqueo de la dirección IP atacante 
\texttt{192.168.1.57}, incorporándola de forma automática a la tabla de bloqueo \texttt{\_\_wazuh\_agent\_drop} de OPNsense. El ataque 
fue ejecutado a las 19:06:50, mientras que la alerta fue correlacionada y visualizada en el \textit{dashboard} de Wazuh a las 19:07:05, 
lo que corresponde a un tiempo de detección aproximado de 15 segundos.

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-19.jpeg}
	\caption{Dashboard de Wazuh con la alerta Http}\label{fig19}
\end{figure}

En la Figura~\ref{fig20} se presenta el registro de la respuesta activa ejecutada por OPNsense tras recibir la instrucción emitida por 
Wazuh. Como resultado, la dirección IP atacante fue incorporada de forma automática a la tabla de bloqueo \texttt{\_\_wazuh\_agent\_drop}, 
impidiendo nuevas comunicaciones provenientes del origen identificado como malicioso.

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-20.jpeg}
	\caption{Registro de respuesta activa en OPNSense}\label{fig20}
\end{figure}

Finalmente, como se evidencia en la Figura~\ref{fig21}, al intentar ejecutar nuevamente el escaneo con Nikto a las 19:09:19, la herramienta 
reportó un fallo al establecer conexión con la dirección IP virtual de la máquina objetivo en el puerto 80 (\texttt{192.168.1.70:80}). 
Este resultado confirma que el bloqueo aplicado por OPNsense fue efectivo, impidiendo cualquier comunicación posterior proveniente de la 
máquina atacante hacia el servicio web expuesto.

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-21.jpeg}
	\caption{Intento de Escaneo Http Posterior al Bloqueo}\label{fig21}
\end{figure}


\subsubsection{Análisis del ciclo de detección}
\noindent

El caso de prueba permitió validar el comportamiento de la arquitectura de defensa en profundidad implementada ante un escaneo web activo 
ejecutado con Nikto sobre la aplicación DVWA. La detección fue realizada por Suricata, el cual identificó patrones de tráfico HTTP 
asociados a actividades potencialmente maliciosas en las URI solicitadas, generando la alerta correspondiente a la firma 
\textit{ET WEB\_SERVER Script tag in URI Possible Cross Site Scripting Attempt}, clasificada dentro de la categoría 
\textit{Web Application Attack} con un nivel de severidad \textit{Major}.

El tráfico generado durante la prueba fue capturado por Suricata a través de la interfaz \texttt{ens36} configurada en modo promiscuo, 
permitiendo la inspección del flujo HTTP de manera paralela al procesamiento perimetral realizado por OPNsense. Posteriormente, las 
alertas fueron enviadas al Wazuh Manager mediante el agente de Suricata, donde la regla personalizada \texttt{100007}, configurada con 
nivel de severidad 12, activó el mecanismo de respuesta activa. Adicionalmente, el evento fue clasificado bajo la táctica 
\textit{Initial Access} y la técnica \textit{Exploit Public-Facing Application} del framework MITRE ATT\&CK, evidenciando una correlación 
consistente entre el comportamiento observado y las tácticas comúnmente empleadas por actores maliciosos durante las fases iniciales de 
un ataque.

El ciclo completo de detección y contención se completó en menos de dos minutos desde la identificación inicial de la actividad maliciosa 
hasta la aplicación efectiva del bloqueo de la dirección IP atacante \texttt{192.168.1.57} en OPNsense. Todo el proceso se ejecutó de 
forma automatizada, sin requerir intervención humana en ninguna de sus etapas, lo que evidencia la efectividad de la arquitectura de 
defensa en profundidad implementada frente a este tipo de amenaza.

El caso de prueba se clasifica como \textbf{Aprobado}, dado que el sistema detectó, correlacionó y mitigó el escaneo web de manera 
automática, activando los mecanismos de respuesta definidos en la arquitectura. Como resultado, Nikto no pudo completar exitosamente un 
segundo intento de reconocimiento sobre la aplicación objetivo, impidiéndose cualquier comunicación posterior desde la máquina atacante. 
De esta manera, se cumplió satisfactoriamente el ciclo de detección, correlación y contención establecido en la metodología de validación.

%-------------------------------------------------------------------------------------------------------------------------------------

\subsection{Caso de Prueba 4 (CP-04) Denegación de Servicios (\DoS) hping3}
\noindent

En el ámbito de la seguridad ofensiva, la generación de tráfico malicioso constituye una técnica ampliamente utilizada para evaluar la 
capacidad de detección y respuesta de los mecanismos de protección implementados en una infraestructura. Los ataques de inundación 
(\textit{flood}) y el envío de paquetes especialmente construidos representan vectores recurrentes en escenarios reales, debido a su 
capacidad para afectar la disponibilidad de los servicios, evadir configuraciones inadecuadas de los dispositivos de seguridad y 
consumir recursos críticos de red~\cite{MITREATTACK2026}. En esta prueba, se simula un ataque de denegación de servicio 
(\textit{Denial of Service, DoS}) mediante el envío masivo de paquetes hacia la máquina objetivo, con el propósito de verificar que la 
arquitectura de defensa en profundidad implementada es capaz de detectar, generar alertas y ejecutar mecanismos de contención de forma 
automatizada.

\subsubsection{Objetivo de la prueba}
\noindent

Evaluar la capacidad de detección y respuesta de la arquitectura de defensa en profundidad implementada ante tráfico malicioso generado 
contra la máquina objetivo. Para ello, se busca verificar que Suricata identifique los patrones de ataque presentes en la capa de red, 
que Wazuh procese y correlacione los eventos generados asignando el nivel de severidad correspondiente, y que el mecanismo de respuesta 
activa ejecute el bloqueo automatizado de la dirección IP atacante en OPNsense, completando el ciclo de detección, correlación y 
contención definido en la metodología.

\subsubsection{Herramienta Utilizada}
\noindent

Hping3 es una herramienta de código abierto orientada a la generación y manipulación de paquetes TCP/IP mediante línea de comandos. A 
diferencia de la utilidad \textit{ping} tradicional, permite construir paquetes personalizados a nivel de protocolo, definiendo 
parámetros como las banderas TCP, el tamaño de la carga útil (\textit{payload}), la frecuencia de envío y la interfaz de origen, entre 
otros~\cite{Hping3Docs}. Debido a estas capacidades, es ampliamente utilizada en pruebas de penetración y ejercicios de seguridad ofensiva para simular 
ataques de denegación de servicio, así como para evaluar el comportamiento de firewalls, sistemas de detección de intrusos y otros 
mecanismos de protección frente a tráfico anómalo. A continuación, se presenta el comando utilizado para el desarrollo de la prueba:

\begin{lstlisting}[language=bash, caption={Comando Hping3 utilizado en la prueba}, label={lst:hping3-flood}]

timeout 7 hping3 -S --flood -p 80 192.168.1.70

\end{lstlisting}

Cada parámetro cumple una función específica, tal como se describe en la Tabla~\ref{tab:parametros_hping3}.

\begin{table}[H]
	\centering
	\caption{Descripción de parámetros utilizados en Hping3}\label{tab:parametros_hping3}
	\renewcommand{\arraystretch}{1.4}
	\begin{tabular}{|p{4.2cm}|p{8.5cm}|}
	\hline
	\rowcolor{gray!25}
	\centering \textbf{Parámetro} & \centering \textbf{Descripción} \tabularnewline
	\hline

	\centering \texttt{timeout 7} &
	Limita la ejecución a 7 segundos
	\tabularnewline
	\hline

	\centering \texttt{-S} &
	Establece el flag SYN en cada paquete simulando el inicio de conexión TCP
	\tabularnewline
	\hline

	\centering \texttt{--flood} &
	Envía los paquetes a la máxima velocidad posible sin esperar respuesta
	\tabularnewline
	\hline

	\centering \texttt{-p 80} &
	Define el puerto destino HTTP, apuntando directamente al servicio web expuesto
	\tabularnewline
	\hline
	\end{tabular}
\end{table}

\subsubsection{Ejecución de la prueba}
\noindent

Esta prueba se ejecutó desde la máquina atacante mediante el lanzamiento de un ataque de inundación SYN utilizando Hping3 contra la 
dirección IP virtual \texttt{192.168.1.70}, expuesta en la interfaz WAN del firewall OPNsense. Para ello, se empleó el comando descrito 
en el apartado anterior, replicando el comportamiento de un atacante cuyo objetivo es degradar la disponibilidad de un servicio web 
mediante el envío masivo de paquetes TCP con la bandera SYN activada.

La prueba, presentada en la Figura~\ref{fig22}, se inició a las 23:14:21 y tuvo una duración de siete segundos. Durante este intervalo, 
Hping3 transmitió un total de 191\,274 paquetes en modo \texttt{--flood} a través de la interfaz \texttt{wlo1} de la máquina atacante. 
Asimismo, se registró una pérdida de paquetes del 100\,\% y tiempos de \textit{round-trip} de 0.0 ms, comportamiento esperado para este 
modo de operación, dado que la herramienta envía paquetes de forma continua sin esperar respuestas por parte del objetivo.

Es importante señalar que OPNsense se encuentra configurado con reglas de filtrado que restringen el tráfico ICMP, razón por la cual no 
resulta apropiado utilizar solicitudes de eco (\textit{ping}) para este escenario. En consecuencia, se empleó la opción \texttt{-S}, que 
permite generar paquetes TCP SYN dirigidos al servicio expuesto, garantizando que el tráfico alcance la máquina objetivo y pueda ser 
inspeccionado por los mecanismos de detección y respuesta implementados en la arquitectura de defensa en profundidad.

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-22.jpeg}
	\caption{Inicio de ataque DoS hacia la máquina objetivo}\label{fig22}
\end{figure}

\subsubsection{Resultados y Evidencias}
\noindent

Los resultados obtenidos durante esta prueba confirman el funcionamiento integral del ciclo de detección y respuesta implementado en la 
arquitectura de defensa en profundidad ante un ataque de inundación SYN.

Como se evidencia en la Figura~\ref{fig23}, Suricata detectó el tráfico generado por Hping3 a las 23:15:20, generando miles de alertas asociadas a 
la firma \textit{SURICATA STREAM 3way handshake SYN resend different seq on SYN recv}. Dichas alertas fueron clasificadas dentro de la 
categoría \textit{Generic Protocol Command Decode} con un nivel de severidad 3, registrando conexiones provenientes de la dirección IP 
atacante \texttt{192.168.1.57} hacia la dirección IP objetivo \texttt{192.168.1.70} a través del puerto 80. Los eventos fueron capturados 
en la interfaz \texttt{ens36}, confirmando que Suricata identificó correctamente el comportamiento anómalo asociado al ataque de 
inundación SYN generado por Hping3.

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-23.jpeg}
	\caption{Inicio de ataque DoS hacia la máquina objetivo}\label{fig23}
\end{figure}

En la Figura~\ref{fig24} se evidencia que Wazuh recibió y correlacionó las alertas generadas por Suricata, visualizándolas en el 
\textit{dashboard} de monitoreo. El evento fue clasificado bajo la táctica \textit{Impact} y la técnica 
\textit{Network Denial of Service} del framework MITRE ATT\&CK, lo que demuestra una correspondencia adecuada entre el comportamiento 
observado y la naturaleza del ataque ejecutado.

Wazuh registró la alerta a las 23:14:30.388 y activó el mecanismo de respuesta activa de forma automática, ordenando el bloqueo de la 
dirección IP atacante mediante su incorporación a la tabla de bloqueo \texttt{\_\_wazuh\_agent\_drop} de OPNsense. Considerando que el 
ataque fue iniciado a las 23:14:21 y que la alerta fue generada a las 23:14:30, el tiempo de detección fue aproximadamente de 9 segundos, 
cumpliendo con los criterios de respuesta establecidos para la arquitectura de defensa en profundidad.

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-24.jpeg}
	\caption{Emisión de alertas en el dashboard de Wazuh}\label{fig24}
\end{figure}

En la Figura~\ref{fig25} se evidencia la ejecución del mecanismo de respuesta activa por parte del agente de Wazuh a las 23:14:30. Como resultado, 
la dirección IP atacante fue incorporada automáticamente a la tabla de bloqueo \texttt{\_\_wazuh\_agent\_drop} de OPNsense, permitiendo 
la contención inmediata de la amenaza sin requerir intervención humana.

\begin{figure}[H]
	\centering
	\includegraphics[scale=0.2]{tablas-images/pruebas/vali-25.jpeg}
	\caption{Respuesta activa OPNsense}\label{fig25}
\end{figure}

Es importante destacar que, una vez aplicado el bloqueo perimetral, no se observa un cambio evidente en la salida del comando ejecutado 
desde la máquina atacante. Este comportamiento se debe a que Hping3, al operar en modo \texttt{--flood}, no espera respuestas por parte 
del firewall ni de la máquina objetivo, limitándose a transmitir paquetes TCP SYN de forma continua y masiva. En consecuencia, la 
ausencia de mensajes de error o de fallos de conexión no constituye un indicador fiable de que el bloqueo no haya sido efectivo.

No obstante, los paquetes SYN generados durante la prueba alcanzaron la infraestructura objetivo y fueron inspeccionados por Suricata, lo 
que permitió la activación de las firmas de detección correspondientes y, posteriormente, la ejecución del mecanismo de respuesta activa. 
Por tanto, la efectividad del bloqueo debe verificarse mediante los registros generados por Suricata, Wazuh y OPNsense, y no a través de 
la salida mostrada por la herramienta atacante.

\subsubsection{Análisis del ciclo de detección}

El caso de prueba permitió validar el comportamiento de la arquitectura de defensa en profundidad ante un ataque de inundación SYN 
ejecutado con Hping3. La detección fue realizada por Suricata, el cual identificó el patrón anómalo de conexiones TCP incompletas 
característico de este tipo de ataque, generando la alerta correspondiente a la firma \textit{SURICATA STREAM 3way handshake SYN resend 
different seq on SYN recv}. Esta firma se activó como consecuencia del envío masivo de paquetes TCP SYN con números de secuencia 
variables, sin completar el proceso de establecimiento de conexión (\textit{three-way handshake}).

El tráfico generado durante la prueba fue capturado por Suricata a través de la interfaz \texttt{ens36} configurada en modo promiscuo, 
permitiendo la inspección del flujo TCP de manera paralela al procesamiento perimetral realizado por OPNsense. Asimismo, se evidenció que 
el ataque fue iniciado a las 23:14:21, mientras que Wazuh correlacionó y expuso la alerta a las 23:14:30, lo que corresponde a un tiempo 
de detección aproximado de 9 segundos. Este resultado se encuentra considerablemente por debajo del umbral crítico de 40 segundos 
establecido en la metodología de validación.

Posteriormente, el mecanismo de respuesta activa fue ejecutado de forma automática, clasificando el evento bajo la táctica \textit{Impact} 
y la técnica \textit{Network Denial of Service} del framework MITRE ATT\&CK. Como resultado, la dirección IP atacante fue incorporada a 
la tabla de bloqueo de OPNsense, interrumpiendo la comunicación maliciosa sin requerir intervención humana.

El ciclo completo de detección, correlación y contención se completó en menos de un minuto desde el inicio del ataque hasta la aplicación 
efectiva del bloqueo de la dirección IP atacante. Este comportamiento demuestra la capacidad de la arquitectura para responder de manera 
autónoma frente a eventos de denegación de servicio basados en inundación de tráfico.

El caso de prueba se clasifica como \textbf{Aprobado}, dado que el sistema detectó, correlacionó y mitigó el ataque de inundación SYN de 
forma automatizada, activando los mecanismos de respuesta definidos en la arquitectura e impidiendo la continuidad del tráfico malicioso 
hacia la máquina objetivo. De esta manera, se cumplió satisfactoriamente el ciclo de detección, correlación y contención establecido en 
la metodología.

%--------------------------------------------------------------------------------------------------------------------------------------

\subsection{Caso de Prueba 5 (CP-05) Ataque SSH con Metasploit}
\noindent

La explotación de vulnerabilidades constituye una de las fases más críticas dentro de un ejercicio de seguridad ofensiva, ya que permite 
al adversario pasar de las actividades de reconocimiento a la obtención de acceso o ejecución de código sobre el sistema objetivo. Los 
\textit{frameworks} de explotación proporcionan un conjunto estructurado de herramientas y técnicas que automatizan el aprovechamiento de 
vulnerabilidades conocidas, reduciendo la complejidad técnica y facilitando la ejecución de pruebas de seguridad 
controladas~\cite{MITREATTACK2026, MetasploitDocs}. En esta prueba, se simula un ataque de explotación contra la máquina objetivo con el 
propósito de verificar que la arquitectura de defensa en profundidad implementada es capaz de detectar, generar alertas y ejecutar 
mecanismos de contención de forma automatizada.

\subsubsection{Objetivo de la prueba}
\noindent

Verificar la capacidad de detección y respuesta de la arquitectura de defensa en profundidad implementada ante un intento de 
explotación ejecutado mediante Metasploit contra la máquina objetivo. Para ello, se busca validar que Suricata identifique las firmas 
asociadas al ataque, que Wazuh procese y correlacione los eventos generados mostrando las alertas correspondientes en el 
\textit{dashboard}, y que el mecanismo de respuesta activa ejecute el bloqueo automatizado de la dirección IP atacante en OPNsense, 
completando el ciclo de detección, correlación y contención definido en la metodología.

\subsubsection{Herramienta Utilizada}
\noindent

Metasploit Framework es una de las plataformas de explotación de vulnerabilidades más utilizadas en el ámbito de las pruebas de 
penetración y la seguridad ofensiva. Desarrollada en Ruby y mantenida por Rapid7, proporciona un entorno modular que integra 
\textit{exploits}, \textit{payloads}, \textit{encoders} y módulos auxiliares, permitiendo ejecutar pruebas de explotación de forma 
estructurada, reproducible y controlada~\cite{MetasploitDocs}. Debido a su amplia cobertura de vulnerabilidades y a su capacidad para 
automatizar distintas fases de un ataque, es una de las herramientas más empleadas en auditorías de seguridad y ejercicios de validación 
de controles defensivos. A continuación, se presenta el comando utilizado para el desarrollo de la prueba:

\subsubsection{Ejecución de la prueba}
\noindent

\subsubsection{Resultados y Evidencias}
\noindent

\subsubsection{Análisis del ciclo de detección}

%--------------------------------------------------------------------------------------------------------------------------------------

\subsection{Caso de Prueba 6 (CP-06) Movimiento lateral (Ataque Interno)}
\noindent

En las fases avanzadas de un ataque informático, una vez obtenido acceso inicial sobre un sistema, el adversario puede intentar expandir 
su presencia hacia otros segmentos de la red mediante técnicas de movimiento lateral. Estas técnicas representan una amenaza 
significativa para las infraestructuras segmentadas, ya que permiten comprometer activos adicionales y aumentar progresivamente el 
alcance del ataque dentro del entorno objetivo~\cite{MITREATTACK2026}. En esta prueba, se simulan técnicas de movimiento lateral 
desde una máquina previamente comprometida hacia otros sistemas del laboratorio, con el propósito de evaluar la capacidad de la 
arquitectura de defensa en profundidad implementada para detectar, generar alertas y contener este tipo de actividad de forma 
automatizada.

\subsubsection{Objetivo de la prueba}
\noindent

\subsubsection{Herramienta Utilizada}
\noindent

\subsubsection{Ejecución de la prueba}
\noindent

\subsubsection{Resultados y Evidencias}
\noindent

\subsubsection{Análisis del ciclo de detección}

\section{Conclusiones de la validación}
\noindent

